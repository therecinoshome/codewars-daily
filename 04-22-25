function uefaEuro2016(teams, scores){
  let winner = scores[0] > scores[1] ? `${teams[0]} won!` : `${teams[1]} won!`;
  if (scores[0] === scores[1]) winner = 'teams played draw.';
  return `At match ${teams[0]} - ${teams[1]}, ${winner}`;
}
